import { createPart } from "./parts/Part";
import { Literal } from "estree";

/**
 * Generates a template part representation for each expression found
 * in the template literal
 *
 * @param expressions The expressions found in the template literal
 */
export const partFactory = (expressions: any[]) => {};

export default partFactory;
